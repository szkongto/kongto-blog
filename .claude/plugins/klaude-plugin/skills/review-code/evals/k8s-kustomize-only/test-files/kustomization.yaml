apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../base

patches:
  - path: patches/configmap-patch.yaml
    target:
      kind: ConfigMap
      name: myapp-config

commonLabels:
  app: myapp
  env: production
